#include <stdio.h>

int main() {
    // arithmetic operators = + - * / % ++ --

    int x = 2;
    int y = 3;
    int z = 0;

    // z = x + y; // use int
    // z = x - y; // use int
    // z = x * y; // use int
    // z = x / y; // use float
    // z = x % y; // use int
    // x++; // use int
    // x--; // use int

    printf("%d", z);

    return 0;
}